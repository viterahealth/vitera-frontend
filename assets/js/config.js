// Change this if your backend runs somewhere other than localhost:8000
// (e.g. once you deploy it). Nothing else in the frontend should
// hardcode a URL -- always import API_BASE_URL from here.
export const API_BASE_URL = "http://127.0.0.1:8000";
//export const API_BASE_URL = "https://vitera-backend-dpks.onrender.com";
